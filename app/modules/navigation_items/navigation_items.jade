ul.navigation-items
  li.item(ng-repeat='item in menuItems | filter: {active:false} | menuOrder: ordered')
    a(ui-sref='{{item.state}}' data-content='{{item.title}}') 
      | {{ item.title | keyWord: keyWord }}
