'use strict'

angular.module('personalSite.wordWrap', [])
  .filter('wordWrap', [function() {
    return function(input) {
      var words = input.split(' ')

      words.map(function(word) {
        
      })

    }
  }]);
